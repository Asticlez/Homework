/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['avatars.githubusercontent.com', 'media1.tenor.com'], // Add 'media1.tenor.com'
    },
  };
  
  export default nextConfig;